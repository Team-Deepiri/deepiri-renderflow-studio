# Copy to .env and fill in values before starting the model worker.

# Path where download_rfir_models.py saves weights (run that script first)
RENDERFLOW_MODELS_DIR=$HOME/renderflow-models

# Redis job queue (must match orchestrator)
REDIS_URL=redis://127.0.0.1:6379/0
